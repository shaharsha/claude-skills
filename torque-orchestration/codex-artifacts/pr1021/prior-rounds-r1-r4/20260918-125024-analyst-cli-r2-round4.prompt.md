# Adversarial review — PR #1021, `torque-admin` analyst read verbs

You are reviewing bytes you did not write. Be adversarial and concrete. A finding must name a file and a mechanism; do not report style preferences, and do not restate the PR's own claims back at me as findings.

## The change

Two read-only CLI verbs over a new service module, plus a fix landed in response to a prior review.

Review the delta `git diff 7d21e5ae..HEAD` — five files:

- `api/services/vault/raw_read.py` — NEW. The §4A route-A mandatory-class row (`api/services/vault/**`). Read this one hardest.
- `api/services/control_plane/artifacts.py` — NEW function `fetch_artifact`.
- `scripts/torque_admin.py` — the CLI wrappers (`cmd_raw_runs`, `cmd_raw_list`, `cmd_raw_fetch`, `cmd_artifact_fetch`) and their parsers.
- `tests/test_vault_raw_read.py`, `tests/test_artifact_fetch.py`.

Supporting context you will need, NOT part of the delta: `api/services/vault/sessions.py` (writes the rows `raw_read` interprets), `db/schema.py` (`client_runs`, `upload_sessions`, `upload_objects`), `lib/vault/client.py`.

## The domain invariant that matters most

Raw client files are immutable evidence in an S3 vault. Every read must address the **recorded `VersionId`** captured at seal time, never a bare key — a presigned PUT can land after a seal, so a bare-key read can return bytes nothing verified. `upload_sessions.state` (`open|sealing|sealed|failed`) is the schema's declared single source of truth for "may this run be analysed".

## What a previous round already found — do NOT re-report these, look for what it MISSED

1. `resolve` admitted candidates on two OBJECT columns (`state == 'verified' and version_id`) while `sessions.seal` writes both *before* deciding the session's fate — so a `failed` or `sealing` run was silently preferred. Now fixed: non-sealed newest is refused by name, `--run` still reads it deliberately.
2. `session_state` was missing from the CLI provenance output. Now added.
3. `raw fetch` defaulted to stdout. Now requires `-o` or `--stdout`.

These three hypotheses were tested and **failed** — do not redo them: the `.get()`-vs-`[]` access on `is_live`/`serve_mode`; duplicate candidates from the `upload_sessions` join; a `DEFAULT_MAX_BYTES` bypass when size is 0.

## Where I most want an independent read

1. **The new refusal in `resolve`.** Is its predicate right, and is it right at the right *place*? Can you construct a recorded row state that still reaches a wrong or misleading answer — including anything involving `other_runs`, the interaction with the same-second tie refusal, `run_id` being supplied, or a path present in several runs with mixed session states? An admission predicate fails through what it ADMITS; attack membership, not the enforcement path.
2. **Anything in this module that can return bytes not pinned to a recorded `VersionId`**, or that can attach a correct-looking provenance record to the wrong bytes.
3. **`fetch_artifact`'s version resolution** — live-vs-newest, `serve_mode`, and whether `served_to_readers` can ever assert something false.
4. **The tests.** Which assertions would still pass if the behaviour they name were broken? Name any test that cannot fail for its stated reason, and any fixture that constrains the code less than its docstring claims.
5. **Error messages that state something the code does not do**, or name a remedy command/flag that does not exist.

Report findings in severity order with file:line and the concrete input or row state that triggers each. If you find nothing in a category, say so explicitly rather than padding.

---

# ROUND 2 — what changed since the round you may be resuming

All five findings of the previous round were confirmed first-party and acted on. **Do not re-report them; review the FIXES, which are new bytes nobody has reviewed:**

1. `resolve` now returns `newer_incomplete_runs` — a newer run holding this path without a verified copy is named rather than silently skipped. It is SURFACED, not refused, deliberately.
2. `fetch(..., to_file=True)` bypasses the terminal-shaped `DEFAULT_MAX_BYTES` ceiling, so the `-o` remedy the size refusal names now works.
3. `served_to_readers` → `served_by_default`, with the comment now recording that an explicit `?v=N` serves any published version.
4. `cmd_raw_fetch` validates the `-o`/`--stdout` choice BEFORE calling `fetch`, so the S3 GET no longer happens ahead of the refusal.
5. The unreadable-path refusal is built from actual object/session states, names every run instead of `candidates[0]`, and no longer recommends `run seal` for a session that cannot be sealed.

Plus `test_the_session_state_space_is_closed_and_this_module_partitions_it`, which asserts the session-state set is closed and that accepted/refused/unreachable partition it.

**Attack the fixes specifically:** is `newer_incomplete_runs` computed over the right set and could it mislead? Can `to_file=True` now stream something unbounded somewhere it should not? Is the partition test's derivation of the state set actually sound, or can it pass while the set is open? Does the new refusal prose state anything the code does not do? And is there any ordering or state combination in `resolve` that still yields an arbitrary or misleading answer?

---

# ROUND 3 — the six round-2 findings are all fixed; review THOSE fixes

Confirmed and acted on: streaming `dest` for `-o` (chunked copy to a temp file + atomic rename, bounded by the recorded size as well as by end-of-stream); `other_runs` computed across all runs instead of the run-scoped query; `>=` instead of `>` for same-second incomplete runs, with an `ordering` field; an argparse mutually exclusive required group for `-o`/`--stdout`; `_why` treating object-state and VersionId as independent axes; and the closure test's claim downgraded to a pin with its limits stated.

Also: `tests/vault_fakes.py`'s `_Body.read` now ADVANCES a cursor — it previously returned the same prefix on every call, so any streaming copy loop ran forever against it.

**This is the last round. Report only what is genuinely defective in these bytes; do not pad.** Specifically: is the chunked copy correct under partial reads and does its size bound have an off-by-one that could truncate a legitimate object? Can the temp-file/rename path leave anything behind, or write outside the intended directory? Is `newer_incomplete_runs`/`other_runs` now correct for every combination of `run_id` supplied/absent? Does the fake's cursor change break any assumption another test in the repo relies on? And does any message still assert something the code does not do?

---

# ROUND 4 — FINAL. All four round-3 findings are fixed.

Fixed: the streamed copy now stops AT the expected length and refuses unless exactly that many bytes arrived (both over and under, `expected = min(size, max_bytes)`); the open-session refusal no longer promises that sealing makes a copy readable and warns that sealing an incomplete run fails it permanently; the state-space test's hard-coded partition is replaced by an assertion that every reachable state has a named behavioural test exercising `resolve`; and the CLI synopsis no longer calls `artifact fetch` bytes "what a reader is SERVED".

**This is the final round and its output will be recorded on the ticket rather than fixed in this PR unless something is genuinely severe.** So: report only defects you would block a merge on, and say plainly if there are none. Do not pad, and do not restate findings from earlier rounds. Focus on the exact-length bound (off-by-one, ranged reads, zero-size objects), the reworded refusal's factual accuracy against `sessions.seal`, and whether any remaining message or docstring asserts something the code does not do.
