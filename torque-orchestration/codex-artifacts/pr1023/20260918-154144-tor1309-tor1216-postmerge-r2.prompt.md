Review the change described below for defects. This is a pre-merge review of the
author's own repository, requested by the author.

<scope>
Repo head under review: 4a17e669 (detached worktree at
/Users/shaharshavit/Projects/torque/.claude/worktrees/pr1023-remedies). It is PR #1023's branch
`me/smp-decision-postpublish` re-merged with `origin/develop` = dc43c427.

The PR's own delta against dc43c427 is 4 paths:

    authored_pages/_recipe/as_of.py
    authored_pages/smp_forecast/build.sh
    authored_pages/smp_simulator/build.sh
    authored_pages/smp_simulator/README.md

Read that delta with:  git diff dc43c427..a4929620

MANDATED WIDER SCOPE — read these in full even though only part is in the diff:

  * the WHOLE directory authored_pages/smp_simulator/, INCLUDING its README.md.
    A house rule (§4A.1) requires that when a review is triggered by a page's build
    artifacts, the review covers that page's entire directory, because the specific
    failure it exists to catch is a change to a page's bytes that silently falsifies a
    sentence in the page's README, where the README was not itself in the diff.
    The question you must answer explicitly: DOES ANYTHING IN THIS CHANGE FALSIFY A
    CLAIM THAT README MAKES?

  * authored_pages/vyb_simulator/README.md around lines 375-390, which is the
    convention this change follows.

  * scripts/doc_citations.py, whose extractor this change is written against.

SECOND MANDATED QUESTION — THE COMBINATION, NOT EITHER SIDE.
A previous round of this same prompt ran at a4929620 and returned no findings. Since then
`origin/develop` moved to 9eb86574 ("split ALM Deal Ops into three pages") and has been merged in,
producing this head. That merge brought SIX mandatory-class paths in from develop:

    api/auth/deps.py
    api/services/alm/dealops.py
    api/services/alm/dealops_context.py
    api/services/alm/live_risk_analysis.py
    api/services/alm/monitoring.py
    api/services/alm/monitoring_management.py

Each SIDE of this merge was reviewed on its own. NOBODY HAS READ THE COMBINATION, and that is the
specific failure this question exists to catch: two changes that are each correct alone but wrong
together, with a clean `git merge` and every test green. Look for interaction between the
merged-in ALM / auth / slot-registry / page-resolution work and this PR's authored-page and
_recipe changes — shared symbols, shared registries, a page-resolution or slot change that alters
what `authored_pages/_recipe/as_of.py` is called with, or a claim in one that the other falsifies.
Compare with:  git diff a4929620..4a17e669
If there is no interaction, say so plainly — "no interaction found" is the expected answer and a
fine one. Do not invent a coupling to fill this slot.
</scope>

Read the diff yourself with git, then read enough of the surrounding files to
judge whether each change is correct in context. Callers, tests, and adjacent
error paths are usually where the answer is. Untracked files are part of the
change and `git diff` will not show them — check `git status --short` and read
those directly.

<intent>
GOAL. Two corrections, both to comments/docstrings. No runtime behaviour is intended to
change anywhere in this diff. If you find a change that DOES alter runtime behaviour, that
is a defect regardless of whether the new behaviour looks better.

CHANGE 1 (authored_pages/smp_{forecast,simulator}/build.sh).
There is no file `publish_preflight.py` under authored_pages/_recipe/ — it is cited by eight
build scripts in this corpus and has never existed in any commit. A previous revision of this
PR inverted the ASSERTION in these two files (from citing the tool to warning that it does not
exist) but left the backticked token spelled with its full repo-relative path.

scripts/doc_citations.py's `backtick_citations` extracts backticked repo-relative paths with a
purely lexical regex that has no notion of negation. So the full path extracted as an
unresolved citation from both files both before and after that earlier fix. The guard is
`.md`-only today (`is_compass_doc` requires `.endswith(".md")`), so it does not currently run on
`.sh` — but the ticket that owns this area, TOR-1154, has as its stated remedy extending that
guard past `.md`, which would then flag both files for prose that is correct.

This change drops the directory prefix from the backticked token in both files, matching the
form authored_pages/vyb_simulator/README.md already uses and states a reason for ("spelled
without its path because the file names nothing tracked"), and records why, so a later reader
does not restore the prefix. The human-readable warning is deliberately KEPT in full.

Six other build.sh files carry the same unresolved citation. They are deliberately NOT touched:
they belong to other pages and to TOR-1154. Do not report them as a defect of this change;
if you think not touching them is wrong, say so as a separate observation.

CHANGE 2 (authored_pages/_recipe/as_of.py).
`_is_projection`'s docstring distinguishes two cases that look identical from inside the
predicate. Its worked example names another directory's artifact (`SMP/simulator`'s
`sim-dispersion`). as_of.py is shared by EVERY authored page, so a change under
authored_pages/smp_simulator/ can falsify that sentence with no file overlap, and no guard
reaches a `.py` docstring in either direction. This change does not remove the example — it
pins it to a named sha and published version as a dated observation, and points at the page
README that owns the claim.

RUNTIME CONTEXT. `authored_pages/*/build.sh` are one-shot developer build scripts run by hand;
they are not services and are not run concurrently. The lines changed in them are `#` comments
inside a block that also contains live shell (`BUILD_BASE="$(git -C "$ROOT" rev-parse
origin/develop)"`). A comment edit that accidentally comments out or un-comments a live line, or
that breaks shell quoting/parsing, would be a real defect — please check the shell still parses
and the block's live statements are untouched.

The `⚠️` and `—` characters are pre-existing house style in these files; they are not new.

SETTLED, NOT UP FOR REVIEW. That `sim-dispersion` draws three unmarked traces of measured cohort
data on an age index has been verified against
api/services/smp/loan_simulator.py (the traces are built directly from
`sd.cohort_roas_dispersion(conn)` over `x=ages`, never through `fm.run`). Both SMP pages are
already published and pinned at version 1; NOTHING in this change may imply that this PR alters
a published page, or make any claim about what a rebuild would produce. If you find a sentence
in the diff that DOES imply either, that is exactly the defect I most want reported.

WHAT I WANT MOST, in priority order:
 1. Any sentence added by this diff that is FALSE, or that contradicts the README of the page it
    describes, or that contradicts scripts/doc_citations.py's actual behaviour.
 2. Any sentence that implies this PR changes a published page, or predicts a rebuild's output.
 3. Any accidental change to live shell or Python behaviour.
 4. A README claim in authored_pages/smp_simulator/ that this change falsifies (§4A.1).

KNOWN AND ACCEPTED: the `.md`-only limit of the citation guard is TOR-1154's to fix, not this
PR's. This PR only avoids planting a false positive for that future fix.
</intent>

<your_constraints>
You are running with the filesystem read-only, so you cannot execute anything
that writes: test runners fail on their own temp directories. Every statement
you make about runtime behaviour is therefore inference from reading, not
observation. Where a finding turns on behaviour you could not observe, say so in
its evidence and let that show in its confidence.
</your_constraints>

<what_counts>
Report a defect when you can name concrete inputs or state that produce a
concrete wrong outcome. Correctness, security, concurrency, data loss, broken
API contracts, unhandled errors, and resource leaks all qualify. So does a
change that contradicts a constraint stated above. For a documentation change,
a statement that is false about the code it describes is a defect of exactly
this kind — name the file and line that refutes it.

Style, naming, formatting, and structural preference do not qualify. Neither
does a pre-existing problem the change does not introduce or worsen. Missing
tests qualify only where an untested path could plausibly be wrong.

Severity is about the consequence if the scenario occurs, confidence is about
whether the scenario is real. Keep them independent — a certain cosmetic issue
is low severity at high confidence, and a suspected data-loss bug is critical at
low confidence. Report both honestly rather than hedging one into the other.

Where the code is genuinely correct, say so and move on. An empty findings list
is a valid outcome and a better answer than a padded one.
</what_counts>

Return your result as JSON conforming to the supplied schema.
