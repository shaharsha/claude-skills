Review the change described below for defects. This is a pre-merge review of the
author's own repository, requested by the author.

<scope>
Repo head under review: 2f71371a (detached worktree at
/Users/shaharshavit/Projects/torque/.claude/worktrees/pr1023-remedies). It is PR #1023's branch
`me/smp-decision-postpublish` re-merged with `origin/develop` = 4a63f768.

THIS IS THE THIRD ROUND ON THIS PR. Rounds 1 (a4929620) and 2 (4a17e669) both returned zero
findings on the same four-path delta. What is NEW and is the reason this round exists is the
MERGE: 96 paths arrived from develop between 4a17e669 and this head, eleven of them in the
repository's "mandatory adversarial review" class. The PR's own content did not change — all four
of its files are byte-identical to round 2's sha (verified blob-by-blob). So do not spend your
budget re-deriving rounds 1 and 2; spend it on the interaction between the merge and the prose.

The PR's own delta against its merge-base is 4 paths:

    authored_pages/_recipe/as_of.py
    authored_pages/smp_forecast/build.sh
    authored_pages/smp_simulator/build.sh
    authored_pages/smp_simulator/README.md

Read that delta with:  git diff 9eb86574..2f71371a -- authored_pages/

The eleven mandatory-class paths that arrived in the merge, which you should read where they
bear on the questions below:

    api/auth/__init__.py  api/auth/config.py  api/auth/deps.py  api/auth/grants.py
    api/auth/session_format.py  api/auth/sessions.py  api/auth/tokens.py
    api/services/computation/batch.py  api/services/computation/broker.py
    db/schema.py
    api/services/smp/historical_analysis.py

Read the full merge-arrived delta with:  git diff 4a17e669..2f71371a

MANDATED WIDER SCOPE — read these in full even though only part is in the diff:

  * the WHOLE directory authored_pages/smp_simulator/, INCLUDING its README.md.
    A house rule (§4A.1) requires that when a review is triggered by a page's build
    artifacts, the review covers that page's entire directory, because the specific
    failure it exists to catch is a change to a page's bytes that silently falsifies a
    sentence in the page's README, where the README was not itself in the diff.
    The question you must answer explicitly: DOES ANYTHING IN THIS CHANGE, INCLUDING THE
    MERGE-ARRIVED BYTES, FALSIFY A CLAIM THAT README MAKES?

  * authored_pages/vyb_simulator/README.md around lines 375-390, which is the
    convention this change follows.

  * scripts/doc_citations.py, whose extractor this change is written against.

  * scripts/preflight.py, which the two build.sh files now name by that exact path.
</scope>

<intent>
GOAL. Two corrections, both to comments/docstrings. No runtime behaviour is intended to
change anywhere in the PR's own diff. If you find a change that DOES alter runtime behaviour,
that is a defect regardless of whether the new behaviour looks better.

CHANGE 1 (authored_pages/smp_{forecast,simulator}/build.sh).
There is no file `publish_preflight.py` under authored_pages/_recipe/ — it is cited by eight
build scripts in this corpus and has never existed in any commit. This change inverts the
ASSERTION in two of those files (from citing the tool to warning that it does not exist) AND
drops the directory prefix from the backticked token, because scripts/doc_citations.py's
`backtick_citations` extracts backticked repo-relative paths with a purely lexical regex that
has no notion of negation — so a full path there reads as an unresolved citation no matter what
the surrounding sentence says. The guard is `.md`-only today (`is_compass_doc` requires
`.endswith(".md")`), but TOR-1154's stated remedy is extending it past `.md`, which would then
flag both files for prose that is correct. The form used matches what
authored_pages/vyb_simulator/README.md already uses and states a reason for.

Six other build.sh files carry the same unresolved citation. They are deliberately NOT touched:
they belong to other pages and to TOR-1154. Do not report them as a defect of this change;
if you think not touching them is wrong, say so as a separate observation.

CHANGE 2 (authored_pages/_recipe/as_of.py).
`_is_projection`'s docstring distinguishes two cases that look identical from inside the
predicate. Its worked example names another directory's artifact (`SMP/simulator`'s
`sim-dispersion`). as_of.py is shared by EVERY authored page, so a change under
authored_pages/smp_simulator/ can falsify that sentence with no file overlap, and no guard
reaches a `.py` docstring in either direction. The example is pinned to a named sha and
published version as a dated observation, and points at the page README that owns the claim.

RUNTIME CONTEXT. `authored_pages/*/build.sh` are one-shot developer build scripts run by hand;
they are not services and are not run concurrently. The lines changed in them are `#` comments
inside a block that also contains live shell (`BUILD_BASE="$(git -C "$ROOT" rev-parse
origin/develop)"`). A comment edit that accidentally comments out or un-comments a live line, or
that breaks shell quoting/parsing, would be a real defect.

The `⚠️` and `—` characters are pre-existing house style in these files; they are not new.

SETTLED, NOT UP FOR REVIEW. That `sim-dispersion` draws three unmarked traces of measured cohort
data on an age index was verified against api/services/smp/loan_simulator.py (the traces are
built directly from `sd.cohort_roas_dispersion(conn)` over `x=ages`, never through `fm.run`).
NOTHING in this change may imply that this PR alters a published page, or make any claim about
what a rebuild would produce.

KNOWN AND ACCEPTED: the `.md`-only limit of the citation guard is TOR-1154's to fix, not this
PR's. This PR only avoids planting a false positive for that future fix.
</intent>

<what_I_want_most_from_THIS_round>
This round exists because of the merge, so these are in priority order:

 1. DOES ANY MERGE-ARRIVED BYTE FALSIFY A SENTENCE THIS PR ADDS? The specific couplings I am
    worried about, each of which you should check against the code rather than reason about:

    (a) The two build.sh now assert, as a positive claim: "`scripts/preflight.py` arrived on
        develop since and is a DIFFERENT tool (cheap pre-PR checks), not a publish preflight."
        Read scripts/preflight.py AS IT IS AT THIS HEAD. Is that description still true? If the
        merge turned it into something that does compare a build base against a publish target,
        the sentence is now false and that is a defect of this head.

    (b) The two build.sh assert "THERE IS NO `publish_preflight.py` under the recipe directory"
        and "the path is in NO commit". Does anything in the merge create, rename, or reference
        such a file into existence?

    (c) api/services/smp/historical_analysis.py CHANGED in this merge. The PR's README and the
        as_of.py docstring both make claims about the SMP simulator page's `sim-dispersion`
        panel — that it is built from `cohort_roas_dispersion`, that its x is an AGE index, and
        that it is measured rather than forecast. Does the merge-arrived SMP change touch
        `cohort_roas_dispersion`, the dispersion panel, or anything that would make those
        sentences false?

    (d) scripts/doc_citations.py — did the merge change the extractor or `is_compass_doc` in a
        way that alters the behaviour CHANGE 1 is written against? If `.sh` is now in scope, say
        so loudly: the PR's whole rationale turns on it.

 2. Any sentence added by the PR's own diff that is FALSE, or contradicts the README of the page
    it describes, or contradicts scripts/doc_citations.py's actual behaviour.

 3. Any sentence that implies this PR changes a published page, or predicts a rebuild's output.

 4. Any accidental change to live shell or Python behaviour, including one introduced by the
    merge resolution itself. The merge reported no conflicts; if you find evidence that it
    silently dropped or duplicated a hunk, that is the most valuable thing you could return.

 5. A README claim in authored_pages/smp_simulator/ that this change — INCLUDING the merge —
    falsifies (§4A.1).
</what_I_want_most_from_THIS_round>

<your_constraints>
You are running with the filesystem read-only, so you cannot execute anything
that writes: test runners fail on their own temp directories. Every statement
you make about runtime behaviour is therefore inference from reading, not
observation. Where a finding turns on behaviour you could not observe, say so in
its evidence and let that show in its confidence.
</your_constraints>

<what_counts>
Report a defect when you can name concrete inputs or state that produce a
concrete wrong outcome. Correctness, security, concurrency, data loss, broken
API contracts, unhandled errors, and resource leaks all qualify. So does a
change that contradicts a constraint stated above. For a documentation change,
a statement that is false about the code it describes is a defect of exactly
this kind — name the file and line that refutes it.

Style, naming, formatting, and structural preference do not qualify. Neither
does a pre-existing problem the change does not introduce or worsen. Missing
tests qualify only where an untested path could plausibly be wrong.

Severity is about the consequence if the scenario occurs, confidence is about
whether the scenario is real. Keep them independent — a certain cosmetic issue
is low severity at high confidence, and a suspected data-loss bug is critical at
low confidence. Report both honestly rather than hedging one into the other.

Where the code is genuinely correct, say so and move on. An empty findings list
is a valid outcome and a better answer than a padded one.
</what_counts>

Return your result as JSON conforming to the supplied schema.
