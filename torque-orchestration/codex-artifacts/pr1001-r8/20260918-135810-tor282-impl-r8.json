{"scan_notes":"Reviewed exact detached HEAD b83cd626b4b294cb4c3a395250f44c06ddd647b8 against origin/develop 4dad3ef237b6240dddd007a02f24219c1835ddcc.\n\nBundle: the current bundle is blob a84dd3677f1dc604e5b08526a1edf0dde8de4271, 703,656 bytes. scripts/build_page.py:190 inlines that file verbatim. The previous bundle had 2 occurrences of `hairline`; the current one has 7. None of the five LR recipes supplies `hairline` as an annotation emphasis, so the new spec.ts refusal is not activated. The recipes and as-of machinery do not pin a bundle hash or depend on renderer behavior. The simulator README correctly identifies a84dd367 as the intended candidate and requires fresh build/render evidence. The overview README contains the one stale bundle claim reported below.\n\nSchema merge: I parsed the DDL string from each merge parent and the head through Python's AST, then executed the head DDL in an in-memory SQLite database; this avoided treating prose comments containing `CREATE TABLE` as schema. The PR parent had 150 tables/53 migrations, develop had 149/54, and the head has 150/54. The head equals the union: `lr_meta` is byte-identical to the PR parent; the `client_memberships` and `invitation_memberships` role CHECK definitions and ENG-5 migration are byte-identical to develop. No side-owned schema change was dropped or altered, and the composed DDL executes successfully.\n\nSimulator README: I read the merged file as a whole and compared both parent diffs. Develop's bundle-verification and control-default repairs remain present, while this PR's D4 edit remains intact. The statement that LR had no capture record is explicitly scoped to the immutable existing artifact, followed immediately by the MERGED/LANDED/REBUILT distinction. I found no merge-induced contradiction, orphaned qualifier, or incompatible assertion.\n\nDate logic: direct execution of the current as_of.py with `lr_meta.report_date = 2026-05-01` produced historical `latest_period=2026-04`, `latest_complete=True`, `value=2026-04`, sentence `Data through April 2026`, and `partial=None`. Overview produced `latest_period=2025-10`, `latest_complete=True`, `value=2025-10`, sentence `Data through October 2025`, and `partial=None`. A boundary control with capture date 2026-04-30 stepped April back to March; 2026-05-01 admitted April. Thus the README date assertions are correct under the current implementation.\n\nOther checks: api/services/slots.py registers `LR/data-qualification` through the computed builder; it is not one of the five authored LR pages. `git diff --check` passed, all changed Python files parsed, and the fact-check range scan passed with 0 unsourced introduced literals across 9 checked files. Per the stated constraint, I did not run the test suite. TOR-1331 and TOR-1332 were excluded as requested.","findings":[{"title":"Incomplete re-ingests leave the previous capture record authoritative","severity":"high","category":"correctness","file":"ingestion/lr_parser.py","line_start":222,"line_end":228,"evidence":"The incomplete-pack branch ends with `return 0` and issues no statement against `lr_meta`. Meanwhile, parse_lr_directory has already run all five source parsers at lines 103-107 and commits unconditionally at line 111. A read-only recorder probe of ingest_meta with only `lr_spend: 1` measured `returned 0`, an empty SQL-statement list, and the expected warning. Existing tests at tests/test_lr_capture_record.py:352-408 begin with an empty `lr_meta`, so they do not exercise retention of an older row. This finding would be falsified if the entry point preflighted the entire pack before any source write, or atomically invalidated the prior capture row when the evidence gate fails; it currently does neither.","failure_scenario":"A database already contains `lr_meta.report_date = 2026-05-01`. An operator later invokes parse_lr_directory on a stale or half-copied directory containing a parseable LR_Spend.csv but missing another pack member. The spend parser executes `INSERT OR REPLACE`, ingest_meta returns 0, and the unconditional commit preserves both the changed spend data and the old May 1 capture row. If that supplied file was captured during April, as_of.py can still certify April as complete using evidence that belongs to the earlier pack, producing a false reader-facing completeness claim.","suggested_fix":"Fail closed before mutating source tables by validating/materializing all five inputs first. If partial source ingestion must remain supported, atomically invalidate the company's existing lr_meta row whenever any source changes but the whole-pack evidence gate fails, and add a regression test that seeds a valid old row before an incomplete re-ingest.","confidence":0.97},{"title":"Overview README names an obsolete rebuild bundle","severity":"low","category":"correctness","file":"authored_pages/lr_overview/README.md","line_start":171,"line_end":172,"evidence":"The README says a rebuild from this tree “would carry `b487592f…`.” I measured HEAD's bundle as a84dd3677f1dc604e5b08526a1edf0dde8de4271 at 703,656 bytes, and scripts/build_page.py:190 reads and inlines that current file verbatim. The claim would be falsified as a finding if HEAD resolved the bundle to b487592f or the builder selected a separately versioned bundle; neither is true.","failure_scenario":"A reviewer or publisher follows this section to identify the renderer requiring new evidence and records or compares against b487592f. A rebuild from b83cd626 actually embeds a84dd367, so the documented provenance points to the wrong renderer and cannot validate the resulting artifact.","suggested_fix":"Replace the claimed current/rebuild blob and byte size with a84dd3677f1dc604e5b08526a1edf0dde8de4271 and 703,656 bytes, retaining the distinction between the immutable published artifact and a future rebuild.","confidence":0.99}],"overall_verdict":"not sound","overall_explanation":"The merge composition, current date derivation, computed-slot claim, simulator documentation, and new renderer behavior all check out. However, the capture-record implementation is not fail-closed after the first successful ingest: a realistic incomplete re-ingest can change LR source data while leaving an earlier capture date authoritative, which can produce a false completeness claim. The overview README also has a low-severity stale bundle identifier.","overall_confidence":0.97}