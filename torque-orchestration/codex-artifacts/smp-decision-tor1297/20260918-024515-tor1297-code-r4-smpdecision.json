{"scan_notes":"Reviewed the entire requested diff, weighting both authored page directories. I compared authored chart specs with retained builder payloads and compiler output, checked simulator pivot/DOM/control behavior, audited both README difference lists, and traced the as-of census through both resolver and renderer doors. The as-of `marks` record and both doors agree on the reviewed inputs; TOR-1309 was deliberately excluded. `rowsFromFigure` reproduces the pivot key/spine, positional lookup is stable for its prefixed keys, payload strings are escaped before `innerHTML`, control bounds precede values, and `divergentAtDefaults` matches the SPA formatter. The factual-literal audit passed. Pytest, binding checks, and fresh browser inspection could not run because the read-only environment provided no writable temporary/session directory; therefore the visual magnitude of the chart defects is unmeasured, although their rendering behavior follows directly from the compiler/runtime contracts.","findings":[{"title":"Forecast uncertainty bands fill down to zero","severity":"high","category":"correctness","file":"authored_pages/smp_forecast/charts.js","line_start":90,"line_end":99,"evidence":"`mk()` does not forward `fill_to`, while the lower-bound area calls at lines 142, 196-197, and 238-248 omit `fill_to: 'previous'`. The builder uses `fill=\"tonexty\"` for these six traces, and the chart compiler translates that to `fill_to: 'previous'`. Without it, ChartView renders an ordinary area against the baseline.","failure_scenario":"Open the default real-client forecast page. The runoff band, book milestone band, and four new-product milestone bands use lower-bound area traces without `fill_to`. Readers see each uncertainty region extend from its lower curve to zero instead of occupying only the interval between upper and lower curves, materially overstating the visual uncertainty. This divergence is also absent from the README's purportedly complete difference list.","suggested_fix":"Forward `fill_to` through `mk()` and set `fill_to: 'previous'` on every authored lower-bound trace corresponding to builder `tonexty`. Extend parity/binding coverage to compare this field.","confidence":0.99},{"title":"Simulator drops two decision reference lines","severity":"high","category":"correctness","file":"authored_pages/smp_simulator/charts.js","line_start":186,"line_end":229,"evidence":"`mechanicsSpec()` and `dispersionSpec()` define only series. The builder adds a committed-facility reference line in `api/services/smp/loan_simulator.py:299-301` and a break-even reference line at lines 335-337. Compiling the retained payload produces those reference-line contracts, but neither appears in the authored specs or README difference list.","failure_scenario":"Load the simulator at its defaults or recompute with another facility value. Readers see a chart titled around two ceilings without the committed-facility ceiling, and the dispersion chart without its 1.0 break-even benchmark. The decision charts therefore omit the principal thresholds needed to interpret them.","suggested_fix":"Add both reference lines. Resolve the mechanics line from the current facility control on every recompute; keep break-even at 1.0. Add parity assertions and declare any intentional remaining divergence in the README.","confidence":0.99},{"title":"Partial recomputes leave the previous scenario visible","severity":"medium","category":"correctness","file":"authored_pages/smp_simulator/charts.js","line_start":631,"line_end":683,"evidence":"The repaint loops skip missing content (`if (!el || !n) continue`, `if (!t) continue`, and `if (!spec) continue`), with the figure comment explicitly saying this “leaves previous one on screen.” The builder has an empty-data response at `api/services/smp/loan_simulator.py:197-201` containing only placeholder KPIs and `sim-empty`.","failure_scenario":"A reader first computes a valid scenario, then a recompute returns the builder's empty-data payload or omits a conditional section. KPIs change to placeholders, but the prior scenario's narratives, tables, and charts remain on screen because the authored page has no `sim-empty` surface and never clears absent components. The page displays mutually inconsistent old and new states.","suggested_fix":"Fail closed when the returned surface is incomplete: clear or hide all scenario-dependent regions and render the returned empty/error state. For conditional sections, explicitly remove or hide components absent from the new payload.","confidence":0.97},{"title":"Forecast prose exposes inaccessible repository references","severity":"medium","category":"api-contract","file":"authored_pages/smp_forecast/body.html","line_start":94,"line_end":94,"evidence":"Reader-facing prose says `See docs/decisions/smp_placebo_finding.md`. Similar internal paths appear at lines 167, 535, 573, and 841, while lines 817 and 848 expose `torque_engine.smp_forecast`. These are literal repository/module identifiers, not links or explanations available inside the published page.","failure_scenario":"An IC or client reader opens the normal forecast page and follows the prose's instruction to consult the cited evidence. They see internal source paths and module names that cannot be opened from the page, so the supposed substantiation is unusable and reads as implementation leakage.","suggested_fix":"Replace the internal identifiers in the builder source with reader-accessible evidence names or concise explanations, then regenerate the authored page. Preserve the same change in builder and authored output rather than editing only the snapshot.","confidence":0.96},{"title":"Simulator test uses an impossible select value","severity":"low","category":"test-gap","file":"frontend/src/pagekit/authored_recompute_echo.test.ts","line_start":602,"line_end":625,"evidence":"The `SMPSIM` fixture offers `none` and `covenant`, then recomputes to `early_amort: 'covenant'`. The real control offers `none`, `6`, `12`, and `18`. The assertions at lines 794-833 do not inspect this select's resulting value.","failure_scenario":"A regression stops correctly writing resolved enum values back to the early-amort select. This suite still passes because its moved value cannot occur in the real page and the select is never asserted; a published page could consequently show a stale or blank control beside newly recomputed results.","suggested_fix":"Reconstruct the real option set, move to a valid value such as `6` or `12`, and assert the select value after initial load and recompute.","confidence":0.98}],"overall_verdict":"not sound","overall_explanation":"The change has two high-severity, default-path chart fidelity failures: forecast uncertainty is rendered with the wrong geometry, and simulator decision benchmarks are omitted. It also permits mixed old/new scenario states on a supported empty response. These should be corrected before merge.","overall_confidence":0.99}