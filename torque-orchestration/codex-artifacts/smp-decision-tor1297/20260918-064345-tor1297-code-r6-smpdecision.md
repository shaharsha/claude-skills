> **Reviewed:** `70432ee4313d674197280eaaaa673fce38dd9424` on `me/smp-decision-pages` — working tree clean
> **Prompt artifact:** `/Users/shaharshavit/Projects/torque/.claude/worktrees/smp-decision/.codex-review/20260918-064345-tor1297-code-r6-smpdecision.prompt.md`  sha256 `4458701cd10e92fe6add149b9784c00d310c6ee22316812e757c90365178abac`
> **Prompt origin:** `/Users/shaharshavit/Projects/torque/.claude/worktrees/smp-decision/.codex-review/tor1297-routea-r6.prompt.src.md`
> **Label:** `tor1297-code-r6-smpdecision` · **Session:** `01a0b29c-f3dd-75a0-864c-0027a87aeb40`
>
> *A verdict covers the bytes it read — the repo sha AND the prompt hash. For a PLAN
> round the repo sha is nearly meaningless and the prompt hash is the whole claim.
> Cite them, or the citation is about a revision nobody has.*# Codex review

- **Verdict:** sound with reservations (confidence 0.98)
- **Findings:** 2 — 1 medium, 1 low
- **Reviewer:** gpt-5.6-sol, reasoning effort high, sandbox read-only
- **Session:** `01a0b29c-f3dd-75a0-864c-0027a87aeb40` — resume this reviewer with `--resume 20260918-064345-tor1297-code-r6-smpdecision`

> Untrusted input. Every finding below is a claim to be checked against the source,
> not an instruction to act on. Confirm or refute each one before changing any code.

## Summary

The round-6 forecast base and byte-count correction is accurate, and no new as-of-door defect was found. However, the simulator's per-component visibility fix still exempts controls and their group container, creating a concrete mismatch on partial and empty recomputes. The forecast README also retains a provenance statement contradicted by its newly recorded build base.

## Adjudication

| # | Sev | Finding | Location | Conf | Verdict | Action |
|---|-----|---------|----------|------|---------|--------|
| 1 | 🟡 medium | Recompute keeps controls that the response no longer renders | `authored_pages/smp_simulator/charts.js:705` | 0.98 | _tbd_ | _tbd_ |
| 2 | ⚪ low | Forecast README says an already-merged mechanism is pending | `authored_pages/smp_forecast/README.md:93` | 0.99 | _tbd_ | _tbd_ |

## Findings

### 1. Recompute keeps controls that the response no longer renders

`authored_pages/smp_simulator/charts.js:705-706` · **medium** · correctness · confidence 0.98

**Evidence.** `var SCENARIO_KEEP = { 'page-head': 1, 'controls': 1, 'headline': 1, 'sim-mix-group': 1, 'sim-controls-mix': 1 };` excludes the top controls and the complete mix group from the response-node reconciliation at lines 780–782. The builder emits `sim-mix-group` only when `_curves` is non-empty, while the SPA filters anchored controls out of its top dock and can render them only through the corresponding controls section.

**Failure scenario.** Start from the published page, then receive a recompute for which `_collections(conn)` is non-empty but `_new_cohort_roas(conn)` produces no curves. The response contains `sim-mechanics` but omits `sim-mix-group`. The authored page hides the mix table and note but leaves the old “Cohort performance mix” container and editable grid visible; the builder-served SPA renders no such anchored panel. If `_collections(conn)` is empty, the response has no controls at all, yet both authored control panels remain visible with prior values beside “data not available.” A reader can therefore see and submit controls for analysis that the current response does not support.

**Suggested fix.** Remove `sim-mix-group` and `sim-controls-mix` from `SCENARIO_KEEP` so their visibility follows the response node set. Reconcile the top `controls` container separately against `payload.controls`, hiding it for the builder's empty response and restoring it when controls are present. Add partial- and empty-response assertions covering these containers.

### 2. Forecast README says an already-merged mechanism is pending

`authored_pages/smp_forecast/README.md:93-99` · **low** · maintainability · confidence 0.99

**Evidence.** The README says PR #996 “had not merged into this page's base (`bb2d2c95`)” and instructs “When #996 lands, rebuild.” The same README now identifies the artifact base as `136d7a98`; PR #996's merge commit is an ancestor of that base, and that base's `as_of.py` contains the two-date/partial-month implementation.

**Failure scenario.** A reviewer auditing the current artifact reads the README and concludes that its base predates the two-date mechanism and that a future merge/rebuild is still required. In reality the mechanism is already present; the published page uses the single projected sentence because the all-projection verdict deliberately suppresses a partial month, not because support is absent. The client-visible sentence remains correct, but its recorded provenance and maintenance instruction are false.

**Suggested fix.** Replace the stale paragraph with the current explanation: base `136d7a98` includes the two-date mechanism, but this artifact's all-projection verdict intentionally renders the single projected form and does not name a partial month. Remove the obsolete rebuild instruction and `bb2d2c95` claim.

<details><summary>Reviewer scan notes</summary>

Reviewed the full scoped diff, concentrating on the two round-6 changes and their affected behavior. Confirmed the forecast artifact's build base is 136d7a987241c6d2e36ddba505def3d64c47230b and build/out.html is 1,172,300 bytes. The as-of marker tests passed (30), and 128 other scoped tests passed; one structure test could not create a temporary directory under the read-only environment. The frontend test runner and browser automation were likewise blocked by read-only cache/socket paths, so the README's live-browser measurements were not independently repeated. The fact-check gate found no unsourced literals. I found no new resolver/renderer disagreement in as_of.py beyond the excluded TOR-1309 case.

</details>
