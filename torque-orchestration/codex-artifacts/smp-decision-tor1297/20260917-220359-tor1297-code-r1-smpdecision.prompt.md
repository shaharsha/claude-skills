Review the change described below for defects. This is a pre-merge review of the
author's own repository, requested by the author.

<scope>
Branch `me/smp-decision-pages` at HEAD `4eb634bb`, worktree
`/Users/shaharshavit/Projects/torque/.claude/worktrees/smp-decision`.

Get the diff with:  git diff origin/develop...HEAD

Two develop merges are in that range and their content is NOT under review. The
change under review is what these commits introduce:

    3c7d0e46  the projection marker and the census
    c1847618  the sweep tool's guards + a golden caption capture
    b61574d6  the bounded `all-projection` verdict

Concentrate on:
  authored_pages/_recipe/as_of.py        (the mechanism)
  authored_pages/_recipe/build_lib.sh    (the one production caller)
  api/services/pages.py                  (the marker constant)
  api/services/smp/forecast.py
  api/services/smp/loan_simulator.py
  api/services/tbr/historical_analysis.py  (the marked traces)
  scripts/measure/tor1297_sweep.py       (a measurement tool, not production)
  tests/test_as_of_projection_marker.py  (the new evidence)
</scope>

Read the diff yourself with git, then read enough of the surrounding files to
judge whether each change is correct in context. Callers, tests, and adjacent
error paths are usually where the answer is. Untracked files are part of the
change and `git diff` will not show them — check `git status --short` and read
those directly.

<intent>
GOAL. An authored HTML page carries a one-line as-of date ("Data through July
2026"). `refuse_edge_beyond_rendered` in `authored_pages/_recipe/as_of.py`
refuses to build a page whose declared date disagrees with the periods the
page's own figures draw. A forecast page draws past its data edge by
construction, so forward-looking traces must be excluded from that comparison.
Before this change they were excluded by matching the trace's NAME against a
regex. This change replaces that with a marker the BUILDER sets, and adds a
verdict for the case where the exclusion empties the period set.

REQUIREMENTS AND INVARIANTS the change must satisfy:
 1. The discriminator may not be a reader-facing string. It must be positive
    (a trace is excluded only if it declares itself), structural, per TRACE
    (never per figure or per page), and invisible to a reader of the page.
 2. Renaming a trace must not move any verdict.
 3. The exclusion must never produce the `no-period-axis` verdict. That verdict
    means "this page draws no dated axis at all" and is publishable; a page
    whose dated series were all excluded is a different fact.
 4. When the exclusion empties a non-empty period set the verdict is
    `all-projection`, and it may publish ONLY IF every marked trace's latest
    period is strictly LATER than the declared edge. A marked trace that stops
    at or before the edge must refuse and be named.
 5. An UNMARKED forward-looking trace must be COUNTED (so the page refuses),
    never silently excluded.
 6. A record written under the old name rule must be refused by the renderer,
    which holds no figure specs and cannot re-derive the period set.

RUNTIME CONTEXT. `as_of.py` runs once per page build, single-threaded, in the
build process, before anything is written. It is loaded STANDALONE by
`build_lib.sh` via `spec_from_file_location`, so it deliberately imports nothing
from `api/`. Its inputs are the page's own builder output (trusted, in-process)
— not user input. A refusal aborts the build and writes nothing. The pages it
gates are a real client's investment-decision pages and are IMMUTABLE once
published, so a false PASS is far more expensive than a false REFUSE.
`rendered_check_problem` is the second door: it runs at render time in
`scripts/build_page.py`, reads only the recorded JSON, and has no database and
no figure specs.

SETTLED DECISIONS, not up for review:
 - The marker rides in plotly's trace-level `meta` key. `go.Scatter` rejects
   undeclared properties, and `scripts/baseline/charts.py` `_TRACE_STYLE`
   already drops `meta`, so this carrier moves no frozen-baseline extract.
 - The key string is spelled twice (`api/services/pages.py` and `as_of.py`)
   rather than imported, because `as_of.py` must not import `api.*`. A test
   binds the two by reading both files as AST.
 - `_FORWARD_TRACE_RE` is kept as an advisory warning only.
 - Renaming builder traces to satisfy the gate was considered and rejected.
 - Adding an actual data series to the pages to satisfy the gate was considered
   and rejected.

DELIBERATELY DEFERRED, not this change's job:
 - The declared edge for these SMP pages resolves from `smp_cohort_age` /
   `smp_cohort_age_seg` / `smp_spend_daily`, while the plotted numbers come from
   `smp_locked_*`. Nothing compares the two. Tracked as TOR-1298.
 - `no-period-axis` remains publishable for a page that genuinely draws no
   period, and such a page's edge is verified against the database alone.
   Tracked as TOR-1237.
 - `scripts/measure/tor1297_sweep.py` is a hand-run measurement tool, not
   production code and not in CI.

CONSTRAINTS HIT WHILE BUILDING:
 - `as_of.py` has a documented history of an import at module scope breaking
   every page's build, which is why it loads `pivot.py` through a file loader.
 - `rendered_periods` had existing callers and tests, so it was kept as a thin
   wrapper over the new `rendered_period_census`.
 - The record gained fields, so `rendered_check_problem` gained a marker check.

KNOWN STATE: 236 tests pass across `tests/test_as_of_projection_marker.py`,
`tests/test_as_of_two_date.py`, `tests/test_as_of_refusals.py`,
`tests/test_as_of_render.py`. No test is skipped or expected to fail.

WHERE THE SPEC LIVES: Linear TOR-1297, and the rules are restated in
`docs/agents/authored-pages.md`.

The parts I would most like checked are the boundary conditions of the new
bound in `refuse_edge_beyond_rendered`, and whether `rendered_check_problem`
can be made to accept a record the resolver would have refused.
</intent>

<your_constraints>
You are running with the filesystem read-only, so you cannot execute anything
that writes: test runners fail on their own temp directories. Every statement
you make about runtime behaviour is therefore inference from reading, not
observation. Where a finding turns on behaviour you could not observe, say so in
its evidence and let that show in its confidence.
</your_constraints>

<what_counts>
Report a defect when you can name concrete inputs or state that produce a
concrete wrong outcome. Correctness, security, concurrency, data loss, broken
API contracts, unhandled errors, and resource leaks all qualify. So does a
change that contradicts a constraint stated above.

Style, naming, formatting, and structural preference do not qualify. Neither
does a pre-existing problem the change does not introduce or worsen. Missing
tests qualify only where an untested path could plausibly be wrong.

Severity is about the consequence if the scenario occurs, confidence is about
whether the scenario is real. Keep them independent — a certain cosmetic issue
is low severity at high confidence, and a suspected data-loss bug is critical at
low confidence. Report both honestly rather than hedging one into the other.

Where the code is genuinely correct, say so and move on. An empty findings list
is a valid outcome and a better answer than a padded one.
</what_counts>

Return your result as JSON conforming to the supplied schema.
