Review the change described below for defects. This is a pre-merge review of the
author's own repository, requested by the author.

<scope>
Repository: this worktree, branch me/lr-simulator, HEAD 515215a51c0d9a4d7cacb03b8f0273814344d000. The change is `git diff 7a74cafc1f3c7426384c90cfb0a676ca55b5d404 HEAD -- authored_pages/lr_simulator/parity_states.py frontend/src/pagekit/torqueRuntimeSections.test.ts scripts/gen_page_body.py tests/test_gen_page_body.py`. The branch also contains merges of origin/develop whose content is not under review. Four files.
</scope>

Read the diff yourself with git, then read enough of the surrounding files to
judge whether each change is correct in context. Callers, tests, and adjacent
error paths are usually where the answer is. Untracked files are part of the
change and `git diff` will not show them — check `git status --short` and read
those directly.

<intent>
Goal: two enablers for porting the LR simulator slot (a real client's underwriting page) to an authored HTML page; the page itself (body.html, charts.js, data.js) is NOT in this change.
(1) scripts/gen_page_body.py emits read-only editablegrid cells with `data-grid-read="<column key>"` so a future charts.js can repaint builder-computed read-only columns by key after a recompute. Invariant: these cells must NOT be serialised into a submitted control value. The runtime (.claude/skills/torque-page-kit/assets/torque-runtime.js, function gridCells) collects `[data-grid-col]`. The vitest test pins that the runtime ignores data-grid-read (its fixture is an <input readonly> on purpose — a <td> has no .value and would be skipped for an unrelated reason); the pytest pins that the generator emits it for the read-only column and not for editable ones. Check whether each test can fail under the mutation it targets, whether the attribute value is correctly escaped, and whether any existing generated page or golden/snapshot changes bytes as a result.
(2) authored_pages/lr_simulator/parity_states.py declares control states a future parity harness will grade, because the slot's section/table SHAPE depends on control values. `shape_signature(payload)` computes the shape rather than declaring it. Check that the declared control values are valid for the slot's control spec (api/services/lr/loan_simulator.py) and that shape_signature reads what its docstring says; flag any state whose values would be clamped/ignored by resolve_values so it cannot reach the branch it names.
Runtime: the generator runs at build time on trusted builder payloads; the page runs in a sandboxed iframe. Published pages are immutable.
Deferred, not this change: charts.js consumer of data-grid-read; the must-fire table consuming parity states; the Offer-B Status label fix (separate PR #984).
</intent>

<your_constraints>
You are running with the filesystem read-only, so you cannot execute anything
that writes: test runners fail on their own temp directories. Every statement
you make about runtime behaviour is therefore inference from reading, not
observation. Where a finding turns on behaviour you could not observe, say so in
its evidence and let that show in its confidence.
</your_constraints>

<what_counts>
Report a defect when you can name concrete inputs or state that produce a
concrete wrong outcome. Correctness, security, concurrency, data loss, broken
API contracts, unhandled errors, and resource leaks all qualify. So does a
change that contradicts a constraint stated above.

Style, naming, formatting, and structural preference do not qualify. Neither
does a pre-existing problem the change does not introduce or worsen. Missing
tests qualify only where an untested path could plausibly be wrong.

Severity is about the consequence if the scenario occurs, confidence is about
whether the scenario is real. Keep them independent — a certain cosmetic issue
is low severity at high confidence, and a suspected data-loss bug is critical at
low confidence. Report both honestly rather than hedging one into the other.

Where the code is genuinely correct, say so and move on. An empty findings list
is a valid outcome and a better answer than a padded one.
</what_counts>

Return your result as JSON conforming to the supplied schema.