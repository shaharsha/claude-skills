---
description: Alias for /worker — start a Torque Sprint worker session
argument-hint: "[TOR-123]"
---

**This command was renamed to `/worker`.** Read
`/Users/shaharshavit/.claude/commands/worker.md` and follow it exactly, with ticket **$ARGUMENTS**.

The alias exists so sessions armed before the rename, and any message or document that still says
`/lane`, keep working. Prefer `/worker` from now on.
